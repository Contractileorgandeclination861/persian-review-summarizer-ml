# ------------------------------------------------------------------
# وابستگی‌های خلاصه‌ساز محلی نظرات فارسی (کاملاً آفلاین)
# ------------------------------------------------------------------

# پیش‌پردازش فارسی
hazm>=0.10

# موتور استخراجی (یادگیری ماشین) — سبک، بدون دانلود مدل
scikit-learn>=1.3
networkx>=3.0
numpy>=1.24

# موتور تولیدی (یادگیری عمیق) — مدل mT5
# اگر فقط از روش extractive استفاده می‌کنی، نصب این سه لازم نیست.
torch>=2.2
transformers>=4.40
sentencepiece>=0.2

# رابط کاربری و داده
streamlit>=1.33
pandas>=2.0
